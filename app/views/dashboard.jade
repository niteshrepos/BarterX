!!! 5
html(lang='en', xmlns='http://www.w3.org/1999/xhtml', xmlns:fb='https://www.facebook.com/2008/fbml', itemscope='itemscope', itemtype='http://schema.org/Product')
  include includes/head
  body
    .wrapper
        .header
          .logo BarterX
          .content
            a(href="articles/new") I Have 
            a(href="articles") I want 
            
          .logout 
            a(href="logout") logout
   - if (articles.length)
      - each article in articles
        .unit
            .myArticle     
                p= article.title
            .suggestions
                input(type="hidden")
                - if (recommendedArticles.length)
                    - each recommendedArticle in recommendedArticles
                        input(type="radio", val=recommendedArticle._id.toString(), name= article.title+"suggestion")
                        p= recommendedArticle.title

            .clr
            .wish
                a(href="/wish") wish

    script
        $("[type=radio]").change(function(){
           console.log($(this).val())
        })                        